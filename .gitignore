Nothing needs to be added to .gitignore since the changes only include HTML files (test_notif.html, test_notif_debug.html) which are source files and not build artifacts, dependencies, or temporary files.